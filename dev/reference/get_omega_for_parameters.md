# Return all diagonal om^2 elements for each non-fixed parameter, as a list

Return all diagonal om^2 elements for each non-fixed parameter, as a
list

## Usage

``` r
get_omega_for_parameters(mod_obj)
```

## Arguments

- mod_obj:

  list object with model information

## Value

list
