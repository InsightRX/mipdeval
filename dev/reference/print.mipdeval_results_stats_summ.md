# Print predictive performance statistics from run_eval()

Print predictive performance statistics from run_eval()

## Usage

``` r
# S3 method for class 'mipdeval_results_stats_summ'
print(x, ...)
```

## Arguments

- x:

  stats summary object in output from
  [`run_eval()`](https://insightrx.github.io/mipdeval/dev/reference/run_eval.md).

- ...:

  further arguments passed to or from other methods.
